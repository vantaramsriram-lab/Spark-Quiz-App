// Central configuration for the quiz application
// Change QUIZ_DURATION_MINUTES to adjust the quiz time limit
// This is the ONLY place you need to change the timer duration

const quizConfig = {
  QUIZ_DURATION_MINUTES: 20,
};

module.exports = quizConfig;
